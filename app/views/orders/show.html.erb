<div class="new">
  <h2>注文履歴詳細</h2>
  <div class="row">


    <div class="col-xs-8">
      <h3>注文情報</h3>
      <div class="alt-table-responsive">
        <table class="table table-bordered">
          <tr>
            <th class="col-xs-4">注文日</th>
            <td class="col-xs-8">2019/11/05(仮) <%#= @order.createdほにゃらら %></td>
          </tr>
          <tr>
            <th class="col-xs-4">発送日</th>
            <td height="80px"class="col-xs-8">
              1500041(仮) <%#= @order.postcode %><br>
              東京都渋谷区神南1丁目19-11 パークウェースクエア2 4階(仮) <%#= @order.address %><br>
              山田花子(仮) <%#= @order.destination %>
            </td>
          </tr>
          <tr>
            <th class="col-xs-4">支払方法</th>
            <td class="col-xs-8">銀行振込(仮) <%#= @order.payment %></td>
          </tr>
          <tr>
            <th class="col-xs-4">ステータス</th>
            <td class="col-xs-8">入金確認(仮) <%#= @order.status %></td>
          </tr>
        </table>
      </div>
    </div>


    <div class="col-xs-4">
      <h3>請求情報</h3>
      <div class="alt-table-responsive">
        <table class="table table-bordered">
          <tr>
            <th class="col-xs-6">商品合計</th>
            <td class="col-xs-6">1,500(仮) <%#= @order.invoice %></td>
          </tr>
          <tr>
            <th class="col-xs-6">配送料</th>
            <td class="col-xs-6">800(仮) <%#= @order.shipping_cost %></td>
          </tr>
          <tr>
            <th class="col-xs-6">ご請求額</th>
            <td class="col-xs-6">2,300(仮) <%#= (@order.invoice).to_i + (@order.shipping_cost).to_i %></td>
          </tr>
        </table>
      </div>
    </div>

    
  </div>
  <div class="row">
    <h3>注文情報</h3>
    <table class="table table-bordered">
      <tr>
        <th width="300px">商品</th>
        <th width="100px">単価（税込）</th>
        <th width="60px">個数</th>
        <th width="60px">小計</th>
      </tr>
      <%#= @orders.each do |order| %>
        <tr>
          <td>いちごのショートケーキ(仮) <%#= order.order_item.item.name %></td>
          <td>500(仮) <%#= order.order_item.prece %></td>
          <td>2(仮) <%#= order.order_item.quontity %></td>
          <td>1,000(仮) <%#= (order.order_item.prece).to_i * (order.order_item.quontity).to_i %></td>
        </tr>
      <%# end %>
    </table>
  </div>
</div>
